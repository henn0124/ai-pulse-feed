# Post Images

Store blog visuals under date-based folders:

- `/assets/images/YYYY-MM-DD/hero.svg` (featured image)
- Optional additional inline visuals in same folder

The daily automation script (`ai-blog-automation/generate-daily-post.sh`) now generates `hero.svg` automatically for each post date.
